name: CI

on:
  push:
    branches: [main]
  pull_request:

env:
  CARGO_TERM_COLOR: always

jobs:
  fmt:
    name: rustfmt
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@master
        with:
          toolchain: "1.98.1"
          components: rustfmt
      - run: cargo fmt --all --check

  cfjs-size:
    # Truncated at the `#` the same way; issue #73.
    name: cf.js size gate (4 KB gzipped)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: oven-sh/setup-bun@v2
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - working-directory: tools/cfjs
        run: |
          bun install --frozen-lockfile
          node size.mjs

  cfjs-push:
    # The browser halves of cf.js: push against stubbed push APIs (issue
    # 183), the reference service worker in a fake
    # ServiceWorkerGlobalScope, and the notification centre against a
    # stubbed /v1/notifications (issue 188). None needs a server, so this
    # does not wait for the wrangler job. What it cannot prove is a real
    # browser delivering a real push — that is the vendor-live leg, issue
    # 186.
    name: cf.js browser halves
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: oven-sh/setup-bun@v2
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - working-directory: tools/cfjs
        run: |
          bun install --frozen-lockfile
          node push.mjs
          node sw.mjs
          node notifications.mjs

  migration-guard:
    # No `#` in the name: GitHub truncates a check-run name there, which
    # registered this as `migrations are never edited (issue` — an awkward
    # string to have to name in branch protection. Issue #34.
    name: migrations are never edited
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          # The guard diffs against the base branch, so it needs history,
          # not the single commit a default checkout fetches.
          fetch-depth: 0
      - name: Never-edit rule
        env:
          BASE: ${{ github.event_name == 'pull_request' && github.event.pull_request.base.ref || 'main' }}
        run: |
          git fetch --no-tags --depth=200 origin "$BASE"
          tools/migration-guard.sh "origin/$BASE"

  clippy:
    name: clippy
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@master
        with:
          toolchain: "1.98.1"
          components: clippy
          targets: wasm32-unknown-unknown
      - uses: Swatinem/rust-cache@v2
      - run: cargo clippy --workspace --all-targets -- -D warnings
      # `fz push send`'s network leg is behind an opt-in feature (issue
      # #184), so the workspace run above never compiles it — and the live
      # proofs (issue #186) would be the first thing to find out.
      - name: fz push send, with its optional transport (issue #184)
        run: cargo clippy -p cratefield-cli --all-targets --features push-send -- -D warnings
      - name: core dependency boundary (ADR 0001)
        run: |
          if cargo tree -p cratefield-core --edges normal | grep -qE '(worker|wasm-bindgen|tokio|reqwest|sqlx|rusqlite) v'; then
            echo "cratefield-core pulled a forbidden dependency:"
            cargo tree -p cratefield-core --edges normal | grep -E '(worker|wasm-bindgen|tokio|reqwest|sqlx|rusqlite) v'
            exit 1
          fi

  test:
    name: test
    runs-on: ubuntu-latest
    services:
      # Postgres 16 for the cratefield-adapter-postgres contract tests
      # (issue #18); tests skip cleanly when the URL is unset.
      postgres:
        image: postgres:16
        env:
          POSTGRES_PASSWORD: postgres
        ports:
          - 5432:5432
        options: >-
          --health-cmd "pg_isready -U postgres"
          --health-interval 5s
          --health-timeout 5s
          --health-retries 10
    env:
      FZ_TEST_POSTGRES_URL: postgres://postgres:postgres@localhost:5432/postgres
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@master
        with:
          toolchain: "1.98.1"
          targets: wasm32-unknown-unknown
      - uses: Swatinem/rust-cache@v2
      - run: cargo test --workspace
      # The workspace run compiles `fz push send`'s network leg out (issue
      # #184), and with it the tests that hold the wiring only that build
      # has — the clock its deadline is enforced through, and the refusal
      # the *other* build prints in its place.
      - name: fz push send, with its optional transport (issue #184)
        run: cargo test -p cratefield-cli --features push-send
      - name: fz migrations apply against the service container (issue #18)
        run: cargo test -p cratefield-cli-acceptance --features postgres
      - name: Error taxonomy drift check (issue #14)
        run: cargo run -q -p cratefield-core --example errors-doc -- --check
      - name: Compatibility matrix drift check (issue #17)
        run: cargo run -q -p cratefield-cli --example compatibility-doc -- --check
      - name: Push environment table drift check (issue #191)
        run: cargo run -q -p cratefield-push-wiring --example push-env-doc -- --check
      - name: Pricing tables drift check (issue #12)
        run: cargo run -q -p cratefield-billing --example pricing-doc -- --check
      - name: Module catalog drift check (issue #139)
        run: cargo run -q -p cratefield-catalog --example catalog-doc -- --check

  doc-commands:
    # No `#` in the name: GitHub truncates a check-run name there (issue #34).
    #
    # Every shell command the README and the getting-started guide tell a
    # reader to run (issue #146). The commands are extracted from the
    # markdown mechanically — tools/doc-commands.sh evals the fence content,
    # so the prose and the tested thing cannot drift — and every fence must
    # be classified: run here, covered by another job, or recorded as a
    # human step. A documented command that stops working fails this job.
    name: documented commands
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - uses: dtolnay/rust-toolchain@master
        with:
          toolchain: "1.98.1"
          targets: wasm32-unknown-unknown
      - uses: Swatinem/rust-cache@v2
      - uses: oven-sh/setup-bun@v2
      - run: tools/doc-commands.sh

  webpush-ntfy:
    # No `#` in the name: GitHub truncates a check-run name there (issue #34).
    name: web push conformance against a real ntfy server
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@master
        with:
          toolchain: "1.98.1"
      - uses: Swatinem/rust-cache@v2
      # `docker run`, not a `services:` container. The image's ENTRYPOINT is
      # `ntfy` with no CMD, so the server needs the `serve` argument — and a
      # service container has no way to supply one (the workflow syntax
      # exposes image, env, ports, volumes and `options`, and `options` is
      # `docker create` flags, not the command). Either way the server is
      # reached at `127.0.0.1:<published port>` from a job running directly
      # on the runner, never by a hostname.
      - name: Is Docker available
        id: docker
        run: |
          if docker info > /dev/null 2>&1; then
            echo "available=true" >> "$GITHUB_OUTPUT"
          else
            echo "::notice::Docker is unavailable on this runner; the ntfy conformance leg is skipped"
            echo "available=false" >> "$GITHUB_OUTPUT"
          fi
      # ntfy v2.28.0, pinned by the manifest-list digest. Apache-2.0 /
      # GPL-2.0, a single Go binary that is a UnifiedPush distributor: the
      # UnifiedPush contract it implements ("a distributor endpoint accepts
      # RFC 8030") is documented at https://docs.ntfy.sh/publish/#unifiedpush
      # and https://unifiedpush.org/spec/definitions/.
      #
      # `NTFY_VISITOR_SUBSCRIBER_RATE_LIMITING=false` is pinned rather than
      # left to the default. With it on — as the public ntfy.sh runs — a
      # UnifiedPush publish answers `507` / code 50701 "cannot publish to
      # UnifiedPush topic without previously active subscriber", even with a
      # subscriber stream held open (verified against the public instance,
      # 2026-09-09). It defaults to false self-hosted, and pinning it stops
      # an upstream default change from turning this leg into a silent
      # no-op. The test asserts a 2xx and fails loudly on that 507.
      - name: Start ntfy (UnifiedPush distributor)
        if: steps.docker.outputs.available == 'true'
        run: |
          docker run -d --name ntfy -p 8080:80 \
            -e NTFY_LISTEN_HTTP=:80 \
            -e NTFY_BASE_URL=http://127.0.0.1:8080 \
            -e NTFY_VISITOR_SUBSCRIBER_RATE_LIMITING=false \
            binwiederhier/ntfy@sha256:6ef4b819f722fccdc036af611c4774cfdc2de821ab74fdd48bbf4c9d6f8973da \
            serve
          for _ in $(seq 1 30); do
            if curl -sf -m 3 http://127.0.0.1:8080/v1/health | grep -q '"healthy":true'; then
              echo "ntfy is healthy"
              exit 0
            fi
            sleep 2
          done
          echo "ntfy never became healthy"
          docker logs ntfy
          exit 1
      # The proof: a real RFC 8030 POST to a server that is not ours, and the
      # body read back out of ntfy's own JSON API and decrypted with the
      # browser-side key the test generated (issue #181).
      #
      # The `grep` is the guard on the guard: the test skips itself when
      # `NTFY_URL` is unset, and a skipped test is a green job that proved
      # nothing.
      - name: Web Push interop against ntfy
        if: steps.docker.outputs.available == 'true'
        env:
          NTFY_URL: http://127.0.0.1:8080
        run: |
          set -o pipefail
          cargo test -p cratefield-adapter-webpush --test ntfy_live -- --nocapture \
            | tee /tmp/ntfy-live.log
          if ! grep -q 'ntfy conformance: decrypted' /tmp/ntfy-live.log; then
            echo "the ntfy leg skipped instead of running: NTFY_URL did not reach the test"
            exit 1
          fi
      # Never the step that fails the job: it is here to explain one that
      # already did.
      - name: ntfy logs
        if: always() && steps.docker.outputs.available == 'true'
        run: docker logs ntfy || true

  # The module conformance kit, run through the reusable workflow so a
  # module built out of tree consumes the identical job (issue #17). The
  # glob covers every module in the workspace, private ones included
  # (ADR 0013), plus the authoring guide's example module (issue #16);
  # the workflow's bash loop word-splits and glob-expands the string.
  conformance:
    name: conformance (module kit)
    uses: ./.github/workflows/conformance.yml
    with:
      module-glob: "crates/module-* examples/module-hello examples/sidecar-module-template"

  workflows:
    name: actionlint
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      # `release.yml` only runs on a push to `main`, so a syntax error in
      # it is invisible: GitHub refuses the file and reports nothing on the
      # PR that introduced it. That is exactly what happened — `secrets` in
      # a step `if:` is not a permitted context, and the release pipeline
      # could not parse from the day it was written.
      - uses: docker://rhysd/actionlint:1.7.7
        with:
          args: -color

  deny:
    name: cargo deny
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: EmbarkStudios/cargo-deny-action@v2

  docs:
    name: cargo doc
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@master
        with:
          toolchain: "1.98.1"
      - uses: Swatinem/rust-cache@v2
      - name: rustdoc -D warnings (crate docs include their READMEs)
        run: RUSTDOCFLAGS="-D warnings" cargo doc --no-deps

  wasm:
    name: worker-build (examples/venture)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@master
        with:
          toolchain: "1.98.1"
          targets: wasm32-unknown-unknown
      - uses: Swatinem/rust-cache@v2
      - name: Install worker-build
        run: cargo install -q --locked worker-build
      # The push provider-token signer must reach wasm32 on its own (issue
      # #178): the venture build below proves it transitively only for as
      # long as an adapter in that venture uses it.
      - name: Build cratefield-push-auth to wasm
        run: cargo build --target wasm32-unknown-unknown -p cratefield-push-auth
      # Same reason for the FCM adapter (issue #179): its headline is that it
      # runs on an isolate, and no venture in this repo enables the `fcm`
      # feature, so the venture build below never compiles it.
      - name: Build cratefield-adapter-fcm to wasm
        run: cargo build --target wasm32-unknown-unknown -p cratefield-adapter-fcm
      # Same reason for the Web Push adapter (issue #180): it is the crate
      # that pulls `aes-gcm`, `hkdf` and `p256/ecdh`, and no venture in this
      # repo wires it yet, so nothing else would notice a dependency that
      # cannot reach wasm32.
      - name: Build cratefield-adapter-webpush to wasm
        run: cargo build --target wasm32-unknown-unknown -p cratefield-adapter-webpush
      # The localisation catalog (issue #190) reaches wasm32 on its own.
      # The venture build below does compile it — it mounts a catalog — but
      # only for as long as that stays true, and this crate's whole claim
      # is that Fluent runs on an isolate: pure Rust, no I/O, and the
      # concurrent memoizer rather than the `Rc`/`RefCell` default.
      - name: Build cratefield-i18n to wasm
        run: cargo build --target wasm32-unknown-unknown -p cratefield-i18n
      - name: Build example venture to wasm
        working-directory: examples/venture
        run: worker-build --release
      - name: Assert shim exists
        run: test -f examples/venture/build/worker/shim.mjs
      # The sidecar template (issue #63) is the Worker a customer builds
      # and deploys themselves; if it cannot reach wasm32 from a fresh
      # clone, the template's whole promise fails at the first `wrangler
      # deploy`. Its own workflow (examples/sidecar-module-template) runs
      # the same gates when the template is extracted; this leg keeps the
      # in-tree copy honest.
      - name: Build sidecar template to wasm
        working-directory: examples/sidecar-module-template
        run: worker-build --release
      - name: Assert sidecar template shim exists
        run: test -f examples/sidecar-module-template/build/worker/shim.mjs

  wrangler-dev:
    name: wrangler dev (examples/venture, local D1)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      # Pinned, because the room smoke test uses the global `WebSocket`, which
      # does not exist before Node 22. Without this the job inherits whatever
      # the runner image ships and the one step that proves issue #103's
      # acceptance dies with `ReferenceError: WebSocket is not defined`.
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - uses: dtolnay/rust-toolchain@master
        with:
          toolchain: "1.98.1"
          targets: wasm32-unknown-unknown
      - uses: Swatinem/rust-cache@v2
      - uses: oven-sh/setup-bun@v2
      - name: Install worker-build
        run: cargo install -q --locked worker-build
      - name: Apply local D1 migrations
        working-directory: examples/venture
        run: bunx wrangler d1 migrations apply venture-example --local
      - name: Start wrangler dev
        working-directory: examples/venture
        run: |
          # Throwaway secrets so the signer and the admin UI are on; the
          # example's mailer stays NotConfigured, nothing is ever sent.
          printf 'HARNESS_SECRET=ci-only-secret-0123456789abcdef-xyz\nADMIN_TOKEN=ci-only-admin-token\n' > .dev.vars
          # `--test-scheduled` exposes /__scheduled, the only way to run the
          # cron entry point against a live isolate. Without it the
          # scheduled half ships untested: it is a different entry point
          # with different ports, and Cloudflare builds no router for it.
          nohup bunx wrangler dev --local --test-scheduled > /tmp/wrangler.log 2>&1 &
          for _ in $(seq 1 60); do
            code=$(curl -s -o /dev/null -w "%{http_code}" -m 3 http://127.0.0.1:8787/__health || true)
            if [ "$code" = "200" ]; then echo "worker ready"; exit 0; fi
            sleep 5
          done
          echo "worker never became ready"; cat /tmp/wrangler.log; exit 1
      - name: Curl assertions
        run: |
          set -e
          echo "-- health"
          curl -sf -m 10 http://127.0.0.1:8787/__health | grep -q '"name":"sample"'
          echo "-- ready"
          curl -sf -m 10 http://127.0.0.1:8787/__ready | grep -q '"ok":true'
          echo "-- surface"
          curl -sf -m 10 -D /tmp/surface.headers http://127.0.0.1:8787/__surface \
            | grep -q '"name":"insert"'
          grep -qi '^etag: "' /tmp/surface.headers
          echo "-- ui page"
          curl -sf -m 10 -D /tmp/ui.headers http://127.0.0.1:8787/ui/waitlist/join \
            | grep -q '<form class="cf-form" data-cf-module="waitlist" data-cf-action="join"'
          grep -qi '^content-security-policy:' /tmp/ui.headers
          echo "-- ui submit (validation error re-renders the form)"
          curl -s -m 10 -o /tmp/ui.body -w "%{http_code}" -X POST http://127.0.0.1:8787/ui/waitlist/join \
            -H 'content-type: application/x-www-form-urlencoded' \
            --data 'email=not-an-email&product=kontinuum' | grep -q '^422$'
          grep -q 'cf-field--invalid' /tmp/ui.body
          echo "-- ui css"
          curl -sf -m 10 http://127.0.0.1:8787/ui/cf.css | grep -q '@layer cf'
          echo "-- reference push service worker, scopable (issue 183)"
          curl -sf -m 10 -D /tmp/sw.headers http://127.0.0.1:8787/ui/sw-push.js \
            | grep -q "self.addEventListener(\"pushsubscriptionchange\""
          grep -qi '^service-worker-allowed: /' /tmp/sw.headers
          grep -qi '^content-type: text/javascript' /tmp/sw.headers
          echo "-- no application server key wired, so the route says so rather than serving an empty one"
          curl -s -m 10 -o /tmp/vapid.body -w "%{http_code}" \
            http://127.0.0.1:8787/v1/notifications/vapid-public-key | grep -q '^404$'
          grep -q 'webpush-not-configured' /tmp/vapid.body
          if grep -qi 'VAPID' /tmp/vapid.body; then
            echo "the refusal named an environment variable"
            exit 1
          fi
          echo "-- admin: login redirect, login, table"
          curl -s -m 10 -o /dev/null -w "%{http_code}" http://127.0.0.1:8787/ui/admin | grep -q '^303$'
          curl -s -m 10 -o /dev/null -D /tmp/login.headers -X POST http://127.0.0.1:8787/ui/admin/login \
            -H 'origin: http://127.0.0.1:8787' -H 'content-type: application/x-www-form-urlencoded' \
            --data 'token=ci-only-admin-token'
          grep -qi '^set-cookie: cf_admin=' /tmp/login.headers
          COOKIE=$(grep -i '^set-cookie:' /tmp/login.headers | sed 's/^[^:]*: *//' | cut -d';' -f1)
          curl -sf -m 10 -H "cookie: $COOKIE" http://127.0.0.1:8787/ui/admin/waitlist/export \
            | grep -q '<table class="cf-table"'
      - name: A transport failure never quotes the request URL (issue #229)
        # The one thing a unit test cannot finish. It can build a
        # `worker::Error`, but only workerd produces the message a real
        # fetch failure carries — and that message is
        # "Fetch API cannot load: <the whole URL>", which for APNs and Web
        # Push *is* the recipient's credential (the path is the token).
        #
        # The sample module's probe fetches two deliberately unreachable
        # destinations whose path is shaped like that credential. The
        # "cannot load" assertion is not decoration: without it this step
        # would still pass on a day workerd stopped naming the URL, and
        # would then be proving nothing.
        run: |
          set -e
          # Generous: the unresolvable destination may sit out the port's
          # own deadline before it answers, and both fetches are sequential.
          curl -sf -m 40 http://127.0.0.1:8787/v1/sample/transport-probe > /tmp/probe.json
          cat /tmp/probe.json
          echo "-- the failure that carries the URL is the one being asserted on"
          grep -q 'Fetch API cannot load' /tmp/probe.json
          echo "-- the origin survives, so the failure is still diagnosable"
          grep -q 'ftp://unreachable.invalid' /tmp/probe.json
          echo "-- and the request target does not"
          if grep -qE 'PROBEDEVICETOKEN|3/device' /tmp/probe.json; then
            echo "the request path reached the error message"
            exit 1
          fi
      - name: A successful outbound fetch through the Worker (issue #132)
        # transport-probe above proves the failure half of the HttpClient
        # port; this proves the success half, which nothing else did. The
        # route fetches a fixed public destination and parses its body, so
        # the assertions below are on what the *Worker* read back — a status
        # from any source would not do. `grep -q` on curl's exit does the
        # rest: a broken fetch port, a changed body shape, or a Worker that
        # stops reaching the network all fail here.
        run: |
          set -e
          curl -sf -m 30 http://127.0.0.1:8787/v1/sample/fetch-probe > /tmp/fetch-probe.json
          cat /tmp/fetch-probe.json
          grep -q '"status":200' /tmp/fetch-probe.json
          grep -q '"h":"www.cloudflare.com"' /tmp/fetch-probe.json
          grep -qE '"colo":"[A-Z]+"' /tmp/fetch-probe.json
      - name: A configured R2 bucket round-trips through the Blob port (issues 105, 132)
        # The R2 adapter's own acceptance note: cargo tests never touch R2,
        # so wrangler dev is the only place `R2Blob` runs. The route puts,
        # reads back, verifies bytes and content type, then deletes — and
        # answers with the verdict, so a silently-broken binding fails
        # here rather than at a customer's first upload.
        run: |
          set -e
          curl -sf -m 30 http://127.0.0.1:8787/v1/sample/blob-probe > /tmp/blob-probe.json
          cat /tmp/blob-probe.json
          grep -q '"round_trip":"ok"' /tmp/blob-probe.json
          grep -q '"key":"probe/' /tmp/blob-probe.json
      - name: Two sockets against the room Durable Object (issue #103)
        working-directory: examples/venture
        # Not curl: a WebSocket upgrade is the one thing it cannot do, and the
        # issue asks for two sockets because everything that matters here is
        # what one sees when the other acts.
        run: node rooms-smoke.mjs
      - name: cf.js embed against the running worker (issue #73)
        working-directory: tools/cfjs
        run: |
          bun install --frozen-lockfile
          node test.mjs
      - name: Example static site against the running worker (issue #77)
        run: |
          (cd examples/venture/site && python3 -m http.server 8788 --bind 127.0.0.1 > /tmp/site.log 2>&1 &)
          for _ in $(seq 1 20); do curl -sf -o /dev/null http://127.0.0.1:8788/ && break; sleep 1; done
          cd tools/cfjs && node site.mjs
          echo "-- insert"
          curl -sf -m 15 -X POST http://127.0.0.1:8787/v1/sample/rows \
            -H 'content-type: application/json' \
            -d '{"email":"ci@example.com"}' | grep -q '"id"'
          echo "-- latest"
          curl -sf -m 10 http://127.0.0.1:8787/v1/sample/rows/latest | grep -q 'ci@example.com'
          echo "-- scheduled: the cron entry point runs on a live isolate (issues 132, 144)"
          # A different entry point with different ports, and Cloudflare
          # builds no router for it — so nothing the fetch assertions above
          # touch proves this half works at all. A module whose scheduled
          # hook reaches for a port the cron path never wired fails only
          # here.
          # A GET: wrangler's own help calls it "visiting /__scheduled".
          curl -sf -m 20 'http://127.0.0.1:8787/__scheduled?cron=*/5+*+*+*+*' \
            -o /tmp/scheduled.body
          echo "-- and it did not fail once inside"
          # A 200 only says the isolate answered. `serve_scheduled` catches a
          # module's error and logs it, so a silent failure looks identical
          # to success from out here — the log is the only place it shows.
          if grep -q 'scheduled module work failed' /tmp/wrangler.log; then
            echo "a module's scheduled hook failed:"
            grep -n 'scheduled module work failed' /tmp/wrangler.log
            exit 1
          fi
          if grep -qiE 'panicked|unreachable executed' /tmp/wrangler.log; then
            echo "the worker panicked:"
            grep -niE 'panicked|unreachable executed' /tmp/wrangler.log
            exit 1
          fi
          echo "all assertions passed"

  wrangler-dev-sidecar-forward:
    # No `#` in the name — branch protection names the check-run string, and
    # GitHub truncates at `#` (issue #34).
    #
    # The live half of the sidecar event-forward contract (issue #258,
    # ADR 0017). `crates/core/tests/sidecar_events.rs` pins the contract
    # against a parking `Defer`, which is a test double: no unit test can
    # answer whether the Workers runtime honours `wait_until` across a real
    # service binding. This job can, and it asserts both halves — because a
    # forward that is never delivered *also* returns promptly, a timing-only
    # check would pass on a completely broken forward.
    name: wrangler dev sidecar event forward (issue 258)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - uses: dtolnay/rust-toolchain@master
        with:
          toolchain: "1.98.1"
          targets: wasm32-unknown-unknown
      - uses: Swatinem/rust-cache@v2
      - uses: oven-sh/setup-bun@v2
      - name: Install worker-build
        run: cargo install -q --locked worker-build
      - name: Apply local D1 migrations
        working-directory: examples/venture
        run: bunx wrangler d1 migrations apply venture-example --local
      - name: Start the sidecar
        working-directory: examples/sidecar-slow-events
        run: |
          # The sidecar carries its own secrets, never the host's; the one
          # shared value is the gateway secret both ends sign with.
          printf 'HARNESS_SECRET=ci-only-sidecar-secret-0123456789abcdef\nSIDECAR_GATEWAY_SECRET=ci-only-gateway-secret-0123456789abcdef\n' > .dev.vars
          nohup bunx wrangler dev --local --port 8799 > /tmp/sidecar.log 2>&1 &
          for _ in $(seq 1 60); do
            code=$(curl -s -o /dev/null -w "%{http_code}" -m 3 http://127.0.0.1:8799/__health || true)
            if [ "$code" = "200" ]; then echo "sidecar ready"; exit 0; fi
            sleep 5
          done
          echo "sidecar never became ready"; cat /tmp/sidecar.log; exit 1
      - name: Start the host with the sidecar bound
        working-directory: examples/venture
        run: |
          # The `-e sidecar` environment adds the mount table and the SLOW
          # service binding; everything else is the default example. The
          # gateway secret must match the sidecar's or the forward arrives
          # unstamped and the mount is dead.
          printf 'HARNESS_SECRET=ci-only-secret-0123456789abcdef-xyz\nADMIN_TOKEN=ci-only-admin-token\nSIDECAR_GATEWAY_SECRET=ci-only-gateway-secret-0123456789abcdef\n' > .dev.vars
          nohup bunx wrangler dev --local --env sidecar > /tmp/host.log 2>&1 &
          for _ in $(seq 1 60); do
            code=$(curl -s -o /dev/null -w "%{http_code}" -m 3 http://127.0.0.1:8787/__health || true)
            if [ "$code" = "200" ]; then echo "host ready"; exit 0; fi
            sleep 5
          done
          echo "host never became ready"; cat /tmp/host.log; exit 1
      - name: A slow sidecar subscriber never holds the host response open
        # Half one of the check. The sidecar stalls every `/__events` eight
        # seconds before answering, so the forward inside the host's
        # `wait_until` genuinely takes eight seconds to complete. If the
        # forward ever leaked out of `wait_until` into the request path,
        # this response would take at least as long — that is the failure
        # the four-second ceiling exists to catch.
        run: |
          set -e
          # `-w %{time_total}` is the assertion; the ceiling is a third of
          # the sidecar's stall. Compared numerically, not with a float
          # literal trick: awk owns the comparison so `0.01` and `9.99` are
          # both judged as numbers.
          RESULT=$(curl -sf -m 20 -D /tmp/probe.headers -o /tmp/probe.body \
            -w '%{http_code} %{time_total}' \
            http://127.0.0.1:8787/v1/sample/sidecar-probe)
          echo "$RESULT"
          echo "$RESULT" | awk '{ if ($1 != 200) { print "probe answered " $1; exit 1 }
                                 if ($2 + 0 >= 4) { print "host response took " $2 "s — the forward is not inside wait_until"; exit 1 } }'
          grep -q '"emitted":"sample.probe"' /tmp/probe.body
          grep -qi '^x-request-id:' /tmp/probe.headers
          RID=$(tr -d '\r' < /tmp/probe.headers | awk 'tolower($1)=="x-request-id:"{print $2}')
          echo "emitted under request id $RID"
      - name: And the forward was in fact delivered
        # Half two, the one a timing-only check lacks: poll the sidecar's
        # evidence endpoint until *this* request's delivery shows up. A
        # broken forward (no binding, wrong name, dead sidecar) leaves the
        # host fast and green above while this goes red — which is the
        # vacuousness that got #62's original acceptance test amended.
        run: |
          RID=$(tr -d '\r' < /tmp/probe.headers | awk 'tolower($1)=="x-request-id:"{print $2}')
          found=""
          for _ in $(seq 1 30); do
            if curl -sf -m 5 http://127.0.0.1:8799/v1/probe/delivered \
                | grep -q "\"last_request_id\":\"$RID\""; then
              found=yes
              break
            fi
            sleep 3
          done
          if [ -z "$found" ]; then
            echo "the sidecar never recorded the delivery (request id $RID)"
            echo "-- sidecar log"; cat /tmp/sidecar.log
            echo "-- host log"; cat /tmp/host.log
            exit 1
          fi
          curl -sf -m 5 http://127.0.0.1:8799/v1/probe/delivered
          echo ""
          echo "forward delivered and heard"
